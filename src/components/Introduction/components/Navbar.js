import React, { useState } from "react";
import "./Navbar.css";

const Navbar = () => {
  

  return (
    <nav className="navbar">
      <span className="navbar__logo">Traveli-<span style={{fontWeight:900,fontSize:'2.7rem',color:'orange'}}>O</span></span>
    
    </nav>
  );
};

export default Navbar;
